# 输入体验与匿名竞赛

- [DONE] 三份用户 TXT 原文逐字接入，合计 42 篇；默认《岳阳楼记》、全文不限时，文章菜单复用 Base UI Combobox，移除页面来源字段。
- [DONE] 实际输入的错误字符红色展示；全文稳定排版，不再切换字符窗口；按行滚动 550ms，尊重减少动画偏好，缓存文字节点以避免计时更新重复渲染长文。
- [DONE] 首次访问通过 HttpOnly Cookie 获取服务器分配的稳定 UUID；WS 校验身份与同源、禁止同浏览器重复连接，仅主动排队者配对；取消、断线、结束清理状态，并支持保留连接再次匹配。
- [DONE] 13 项单元／真实双客户端集成测试与类型检查通过；正式静态构建通过（需沙箱外临时监听端口）。

- [DONE] 功能提交 ca53013 已推送；GitHub CI 33865884620 全部通过，包括 13 项测试、类型检查、静态构建、部署脚本验证、Docker 构建与容器健康／首页检查。构建页面确认默认《岳阳楼记》，无原生 select 或来源标签。
